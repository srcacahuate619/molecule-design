# =============================================================================
# Dependencias pip del microservicio de ML Rescoring
# =============================================================================
# ODDT y RDKit se instalan via conda-forge (ver Dockerfile.rescoring).
# Este archivo contiene solo las dependencias disponibles en PyPI.
# =============================================================================

# ── Web Framework ────────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.11.3

# ── ML / Scientific ─────────────────────────────────────────────
xgboost==2.1.4
scikit-learn==1.6.1
numpy>=1.26,<2.1
scipy>=1.12,<1.15
joblib==1.4.2
shap==0.46.0

# ── HTTP client (para health check interno) ─────────────────────
httpx==0.28.1

# ── Logging ──────────────────────────────────────────────────────
structlog==25.2.0

pydantic-settings
